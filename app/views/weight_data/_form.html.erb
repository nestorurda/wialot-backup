<%= form_with(model: weight_datum) do |form| %>
  <% if weight_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(weight_datum.errors.count, "error") %> prohibited this weight_datum from being saved:</h2>

      <ul>
        <% weight_datum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
